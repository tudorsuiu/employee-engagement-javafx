package com.example.demo.domain.models;

public enum Badge {
    // < 50 points
    Potential,
    // >= 50 points
    Explorer,
    // >= 75 points
    Innovator,
    // >= 125 points
    Challenger,
    // >= 175 points
    Visionary,
    // >= 250 points
    Mastermind,
}
